# 领域术语

本文件是术语表，只定义概念，不记录实现方案与技术决策。

## 宿主既有概念

这些术语由 DSH 宿主定义，本仓库沿用其含义，不得重新解释。

### 工作区（Workspace）

用户工作目录的持久记录：一个建立在规范路径之上的稳定 id、一个显示标题，以及归属于它的会话的有序账本。工作区**不拥有**目录本身，也不拥有会话的日志——移除一条工作区记录不删除任何文件。

### 会话（Session）

一段对话的仅追加事件日志，加上一个创建时确定、此后不可变的头部。头部中的工作目录（cwd）是会话与工作区之间唯一的结构性纽带。一个会话在结构上至多属于一个工作区。

### 未分组（Ungrouped）

不隶属于任何现存工作区的会话所在的位置。它不是一个容器，而是"所有现存工作区账本的补集"这一推导结果。工作区被移除后，其会话即落入未分组。

避免的同义词：孤儿会话、游离会话。

### 归档（Archive）

把一个会话从所有分组展示面（工作区分组、未分组、扁平列表、搜索结果）上隐藏。归档**不搬移数据、不修改会话日志、不改变工作区归属**——被归档的会话仍然占着它在工作区账本里的位置。

归档是一个纯粹的可见性概念。不要把它说成"移动到归档目录"或"打包存档"。

### 归档集合（Archive Set）

被归档会话的全体。它是全局的，横跨所有工作区，也包含不属于任何工作区的会话。

## 本仓库引入的概念

### 归档区（Archive Area）

面向用户的一个位置，用于查看归档集合中的会话，并对它们执行取消归档与删除。

「归档区」是一个**展示面**，不是一个存储位置。会话进入归档区不意味着它的数据被搬到了别处。

### 取消归档（Unarchive）

归档的逆操作：把一个会话从归档集合中移出，使它重新出现在原本的分组展示面上。因为归档从不改变工作区归属，取消归档后会话回到它原来的工作区和原来的位置，而不是落入未分组。

避免的同义词：恢复、还原（这两个词在删除语境下另有含义，混用会产生歧义）。

### 删除（Delete）

不可逆地移除一个会话的日志。删除是终点，没有回收站、没有撤销窗口。

删除与移除工作区是两件不同的事：移除工作区只丢弃一条注册记录，任何会话日志都不受影响。不要把「删除工作区」当作「删除其中的会话」的说法。
